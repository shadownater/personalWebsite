/**
 * Created by Jill - Offline on 2017-08-19.
 *
 */


//this function will make the carousel the appropriate min-height for the image
/*
var myImages = $(".imageResizing");

var maxHeight = 0;

//store the height of the largest image, then use that to set the min height of the carousel
for(var i = 0; i < myImages.length; i++){
    if(myImages[i].height > maxHeight){
        maxHeight = myImages[i].height;
    }
}

$("#picCarousel").css('min-height', maxHeight);
    */